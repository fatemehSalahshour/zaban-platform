# راهنمای ادامه‌ی کار — پلتفرم زبان کنکور ارشد

این فایل را در چت جدید آپلود کن و بگو «این پروژه‌ی من است، ادامه بده».
همراهش **حتماً** این‌ها را هم بفرست (نسخه‌ی فعلی روی دیسک، نه نسخه‌ی قدیمی):
- `public\vocab-platform-v69-wired.html` (نه فایل demo!)
- `public\js\app-bootstrap.js`
- و هر فایل PHP که قرار است رویش کار شود

آخرین به‌روزرسانی: ۳۰ شهریور ۱۴۰۵

---

## معرفی پروژه

پلتفرم مطالعه‌ی لغات و آزمون آزمایشی زبان کنکور ارشد. Laravel 13 + یک
فایل HTML تک‌صفحه‌ای (Vanilla JS) که با API صحبت می‌کند. روی WAMP،
ویندوز، لوکال. **هنوز روی سرور واقعی نرفته.**

**دو دیتابیس:** `zaban_platform` (اصلی) و `azmoon` (پلتفرم آزمون، منبع سؤال‌ها).

**من (کاربر) برنامه‌نویسم، نه مدیر پروژه.** راهنمای تست را قدم‌به‌قدم و
با اسم دقیق دکمه‌ها بنویس؛ کد و مسیر را از متن فارسی جدا بگذار.

### اجرا
```
cd C:\wamp64\www\zaban
"C:\wamp64\bin\php\php8.3.14\php.exe" artisan serve
```
مرورگر: `http://127.0.0.1:8000/dev-login` ← می‌رود به `/zaban#rank` (داشبورد)
پنل مدیریت: `http://127.0.0.1:8000/zaban-admin`

---

## ✅ انجام شده در این دور (تست شده مگر خلافش نوشته شده)

1. **دکمه‌ی «⚙ مدیریت»** کنار داشبورد، فقط برای admin/manager/editor. ✅
2. **گزارش‌ها سمت سرور**: جدول‌های `reports` و `report_messages` (migration)،
   `ZabanReportController` (کاربر)، `ZabanAdminReportController` + صفحه در
   پنل با سایدبار و عدد گزارش‌های باز. ✅
3. **اطلاعیه‌ها از سرور**: جدول‌های `announcements` و `announcement_reads`،
   صفحه‌ی نوشتن/انتشار در پنل، عدد زنگ = خوانده‌نشده‌ی همین کاربر. ✅
4. **سؤال ساختگی حذف شد**: `makeQ` فقط سؤال واقعی بانک؛ برای دفترچه‌ی ناقص
   پیام «هنوز وارد نشده». ✅ (رجوع به یادداشت تست ۲ پایین)
5. **پاسخ سؤال‌ها از سرور**: مسیر `GET /api/answers?ids=` (دسته‌ای، ۴۰ تا)،
   قاعده‌ی قفل فیدبک در `answerGate` مشترک. کش `ANS` در صفحه. پاسخ تشریحی
   واقعی به‌جای متن قالبی. ✅ تست ۱
6. **کرش «k.split is not a function»**: سرور کلید سؤال (`k`) را در دک/منتخب/
   یادداشت می‌فرستد؛ عددهای خراب قبلی موقع باز شدن دور ریخته می‌شوند. ✅
7. **طراحی پنجره‌ی سؤال**: گزینه‌ها دیگر حرف‌به‌حرف نمی‌شکنند، متن سؤال
   چپ‌چین، «null» حذف. ✅
8. **⚠️ نمره‌ی آزمون روی سرور یکی جابه‌جا بود** (این دور درست شد، تست نشده):
   صفحه گزینه را از صفر ذخیره می‌کند (گزینه‌ی ۱ = 0)، `grade()` از یک
   می‌خواند. گزینه‌ی ۱ «نزده» حساب می‌شد و بقیه یکی جابه‌جا. حالا +1 می‌شود.
   - **آزمون‌های قبلی داخل دیتابیس با نمره‌ی غلط ذخیره شده‌اند.** چون فقط
     دیتای تست لوکال است، ساده‌ترین کار پاک کردنشان است:
     ```sql
     DELETE FROM exam_answers WHERE attempt_id IN (SELECT id FROM exam_attempts WHERE user_id = 1);
     DELETE FROM exam_attempts WHERE user_id = 1;
     ```
9. **کارنامه از تاریخچه** (این دور، تست نشده): قبلاً تب آزمون را می‌خواباند
   (تاریخچه `bySec` و `used` نداشت). مسیر تازه‌ی `GET /api/exam/{id}/result`؛
   `report()` حالا نتیجه‌ی بخش‌ها را واقعاً حساب می‌کند (قبلاً همیشه خالی)،
   و نام بخش‌ها فارسی است (قبلاً «vocab»).

---

## تست‌های دور اول (تاریخی — همه انجام شد؛ وضعیت فعلی در «📍 وضعیت فعلی» بالا)

**تست ۲ — دفترچه‌ی ناقص: لازم نیست.** در دفترچه‌ی ۱۳۸۸ آی‌تی نه سؤالی
وارد شده نه حتی یک وقوع کلمه («۰ کلمه با این فیلتر»)، پس آن دفترچه اصلاً در
«تست‌های زبان» نمی‌آید. پنج دفترچه‌ی ناقص پنل همه «وارد شده: ۰» دارند. سؤال
ساختگی فقط از مسیر وقوع کلمه ساخته می‌شد؛ کد حذفش شده و راه دیگری ندارد.
اگر روزی دفترچه‌ای «نیمه‌کامل» شد (بعضی سؤال‌ها وارد، بعضی نه)، همان‌جا
پیام «هنوز وارد نشده» باید دیده شود.

**تست ۳ — کارت سؤال در مرور: ✅** (پاسخ درست و «پاسخ شما غلط بود» درست آمد.)

**تست ۴ — نمره‌ی آزمون: ✅** ۱۳ پاسخ (چندتا گزینه‌ی ۱) ← ۲ درست، ۱۱ غلط،
۱۲ نزده؛ درصد ‎−۶٫۷ = (۳×۲−۱۱)/(۳×۲۵)؛ بخش‌ها با اسم فارسی و درصد درست
(وکب ‎−۱۴، کلوز ۲۲، پسیج ‎−۹). ✅ از تاریخچه هم درست باز می‌شود.

**تست ۵ — قفل فیدبک: ✅** پاسخ لو نرفت. دو ایراد نمایشی دیده و درست شد:
گزینه‌ی انتخاب‌شده قبل از معلوم شدن پاسخ قرمز («غلط») می‌شد، و پیام قفل
دوبار — یکی در جعبه‌ی سبزِ «پاسخ درست» — می‌آمد. حالا یک پیام، در جعبه‌ی
خنثی، و گزینه فقط «انتخاب‌شده».

**تست ۴ و شمارش دک:** ✅ هر دو بسته شد (دک ۹۶۷ کلمه + ۱ سؤال، روی سرور هم همین).

---

## 📍 وضعیت فعلی (۳۱ شهریور ۱۴۰۵)

**همه‌ی کدنویسی فهرست اصلی تمام و تست شده؛ ۵۸ تست خودکار سبز.** آنچه مانده:

**کدنویسی**
1. جدا کردن کد — قدم ۲: HTML به Blade (نسخه‌ی CSS خودکار، بی‌نیاز از `?v=` دستی)
2. جدا کردن کد — قدم ۳: جاوااسکریپت بخش‌به‌بخش در فایل‌های جدا
3. تأیید دستی دو اصلاح موبایل (هدر، نوار سال‌ها)

**کوچک / تصمیم با کاربر**
4. ✅ تم تیره/کم‌نور — سه لایه اصلاح شد: (الف) پنج جعبه‌ی رنگ‌ثابت؛ (ب) **پنج متغیر رنگی**
   (`--accent`, `--accent-soft`, `--blue`, `--blue-soft`, `--blue-line`) که فقط در `:root`
   بودند و در تم تیره بازتعریف نمی‌شدند (ریشه‌ی جعبه‌ی سبز «پیش‌بینی کنکور»)؛ (ج) عناصر
   `background:var(--ink)+color:#fff` که در تم تیره روشن‌با‌متن‌سفید می‌شدند: «مشاهده‌ی پاسخ»،
   `.qbtn.on`، `.htabs button.on` و **پیام‌های کوتاه (toast)** که اصلاً خوانده نمی‌شدند.
   به‌علاوه سطح‌های متن‌دار (پسیج، متن دفترچه، یادداشت، جعبه‌ی پاسخ آزمون، گزارش‌ها) و
   ردیف‌های hover که در تم تیره برق می‌زدند.
5. ✅ سقف کارت تازه در روز: پیش‌فرض در پنل (`zaban_meta.new_per_day`) + انتخاب هر دانشجو در
   پروفایل (`zaban_profiles.new_per_day`، ۵ تا ۱۰۰؛ خالی = پیش‌فرض مدیر). `/me` عدد مؤثر را می‌دهد.
6. (اختیاری) آپلود اکسل کلمات سال تازه از پنل

**غیرکدنویسی — روز انتقال** (`docs/DEPLOY.md` قدم‌به‌قدم، در پایان `zaban:preflight`)
7. سمت auth: `docs/چکلیست_auth_انتقال_زبان.md`
8. ایران کیش: پایانه، پذیرنده، کلمه عبور، کلید PEM، **ثبت آی‌پی سرور**
9. سرور: `.env`، migrate، انتقال جدول‌های محتوا، کاربر فقط‌خواندنی azmoon، cron، کش، https
10. پنل روی سرور: قیمت‌ها، تاریخ کنکور، سال دمو، همگام‌سازی سؤال‌ها؛ یک خرید واقعی با کمترین مبلغ

**تیم محتوا:** ۵ دفترچه‌ی ناقص + سؤال‌های ۱۴۰۵

---

## 🗺 نقشه‌ی راه — ترتیب قطعی کارهای باقی‌مانده

(تأیید کاربر، ۳۰ شهریور ۱۴۰۵. جزئیات فنی هر مورد پایین‌تر.)

**۱. آمار سؤال (هـ + ز) — ✅ انجام و تست شد**
- `GET /api/crowd`: فقط رشته‌های خریده‌شده؛ سؤال‌ها از `exam_answers` (آزمون‌های
  تمام‌شده)، کلمات از آخرین امتیاز هر نفر در `review_logs`. کش ۱۰ دقیقه (لوکال ۵ ثانیه).
  **فقط نرخ‌ها** — توزیع گزینه‌ها عمداً نیست (از آن پاسخ همه‌ی بانک حدس زده می‌شد).
- `GET /api/question/{id}/stats`: توزیع گزینه‌های یک سؤال، پشت `answerGate`.
- `POST /api/question/{id}/attempt` + جدول `question_attempts`: تمرین شخصی؛
  درستی را سرور حساب می‌کند. `/me` → `qatt`.
- حداقل نمونه `crowdMin()`: لوکال ۱، سرور ۵ (جلوی لو رفتن پاسخ یک نفر).
- تب «آمار جمعی» سؤال‌های همه‌ی رشته‌های خریده را بارگذاری می‌کند، نه فقط رشته‌ی پروفایل.

**باگ قدیمی throttle — ✅ درست شد:** همه‌ی `throttle:N,1`ها در لاراول یک شمارنده‌ی
مشترک به‌ازای کاربر داشتند (کلید فقط از user id، نه مسیر) و هر درخواست دوبار
شمرده می‌شد. heartbeat با سقف ۶ تقریباً همیشه ۴۲۹ می‌گرفت (زمان مطالعه همیشه
۰۰:۰۰)، `/crowd` بعد از بارگذاری صفحه رد می‌شد. حالا محدودکننده‌های نام‌دار در
`AppServiceProvider` (`zaban-api`, `zaban-crowd`, ...)، هر مسیر شمارنده‌ی خودش.
**قانون:** در این پروژه دیگر `throttle:عدد,1` ننویسید؛ یک محدودکننده‌ی نام‌دار بسازید.

**`/me` مقاوم شد:** `qatt`، `activity` و `new_today` داخل `rescue()`؛ خطای
یکی فقط همان بخش را خالی می‌کند و در لاگ ثبت می‌شود، نه کل پلتفرم.

**۲. فیلتر «تست‌های زبان» (ط) — ✅ انجام شد.** `placeFilters(t)` در
`openTab`: همان `#filterPanel` بین `#filterHost` و `#testsHost` جابه‌جا می‌شود؛
وضعیت (`F`) مشترک می‌ماند. مرتب‌سازی کلمات (`#sort`) در تب تست‌ها پنهان است.

**۴۲۹ بعد از چند تب:** حلقه‌ای پیدا نشد (شکارچی حلقه در `app-bootstrap.js`:
هشدار Console اگر یک مسیر بیش از ۱۰ بار در ۱۰ ثانیه). سقف کلی `zaban-api` از
۱۲۰ به ۳۰۰ در دقیقه رفت (مشترک بین همه‌ی تب‌های یک کاربر). پیام ۴۲۹ هنگام
بارگذاری حالا «یک دقیقه صبر کنید» است نه «بارگذاری ناموفق».

**۳. ورود (SSO) — ✅ SSO را کاربر نوشت و تحویل داد؛ اتصال‌های سمت پلتفرم ✅ انجام و تست شد**

**📋 روز انتقال: `docs/چکلیست_auth_انتقال_زبان.md`** — تغییرات سرور auth به ترتیب
(`sso_clients.php`، کلاینت OAuth، secret، `.env` فقط روز انتقال، تست خروج مرکزی،
مجوز `oauth-public.key`). **حتماً همان فایل را قدم‌به‌قدم دنبال کن.** تنظیمات خود
زبان در `INSTALL.md` بسته‌ی `zaban-sso` است (در این پروژه نیست، پیش کاربر).

ساختار SSO (نوشته‌ی کاربر):
- `SsoController` (redirect/callback/logout با state + nonce + PKCE، ورود بی‌صدا
  `prompt=none`)، `SsoBackchannelController`، `SsoAutoLogin`، `SsoSessionGuard`
  (خروج مرکزی با `users.sso_logout_at`؛ برای `api/*` جواب ۴۰۱)،
  `Services/Sso/SsoClient` و `SsoUserResolver`.
- **تطبیق کاربر با `users.global_uid`، بعد `users.mobile`، بعد ساخت حساب تازه**
  (`type = student`). `users.id` محلی ثابت می‌ماند — هشدار قدیمی «users.id باید
  با SSO یکی باشد» **دیگر درست نیست**.
- **ادمین‌ها:** حساب ادمین باید از قبل با `mobile` درست در `users` باشد تا اولین
  ورود SSO به همان حساب لینک شود؛ وگرنه حساب تازه با `type = student` ساخته می‌شود.
- `/dev-login` فقط در `APP_ENV=local`؛ روی سرور `APP_ENV=production` حتماً.

اتصال‌های سمت پلتفرم (این دور):
- دکمه‌ی «خروج از حساب» در پروفایل و در سایدبار پنل مدیریت.
- صفحه‌ی `home` (`resources/views/home.blade.php`) به‌جای صفحه‌ی پیش‌فرض لاراول:
  دکمه‌ی ورود و نمایش `sso_error`؛ کاربر واردشده مستقیم به `/zaban`.
- رایانه‌ی مشترک: `/me` حالا `uid` می‌فرستد؛ اگر داده‌ی `localStorage` مال کاربر
  دیگری باشد پاک می‌شود؛ خروج هم پاکش می‌کند (`ZABAN.logout()`).
- `backchannel-logout`: `throttle:60,1` ← محدودکننده‌ی نام‌دار `sso-backchannel`.
- ستون `users.mobile` حالا هست ← پروفایل موبایل را نشان می‌دهد.

**۴. صفحه‌ی خرید + درگاه ایران‌کیش — ✅ انجام و با درگاه آزمایشی تست شد** (سفارش ۱: ce+cs، ۱٬۲۰۰٬۰۰۰ تومان، paid)
- `/buy` حالا «خرید و پرداخت‌ها»: رشته‌های فعال با تاریخ اعتبار، رشته‌های دمو، فرم خرید،
  و فهرست پرداخت‌ها (تاریخ شمسی با `toLocaleDateString('fa-IR')` مرورگر). ✅ تست شد
- خط رشته‌ها بالای پلتفرم: رشته‌ی خریده ✓، دمو «دمو»، نخریده لینک «خرید» ← `/buy?exam=`. ✅ تست شد
پیاده‌سازی طبق راهنمای رسمی ایران کیش (IPG_TECHNICALGUIDE بازنگری ۹، ۱۳۹۹/۱۰/۰۹).
مراحل AES و SHA پاکت دیجیتال با مثال خود راهنما آزموده شد و منطبق است.
- `/buy` صفحه‌ی مستقل Blade (کاربر بی‌دسترسی رابط را نمی‌تواند بارگذاری کند؛ پی‌وال
  قبلاً به `/buy` می‌رفت که وجود نداشت). قیمت زنده از `/api/buy/quote`.
- `App\Services\Payment\IranKish`: tokenize / confirm / inquiry / envelope.
- `App\Services\Payment\Checkout`: ماشین حالت `pending → verifying → paid|failed|expired`؛
  گذر از pending اتمی؛ دسترسی فقط بعد از confirm با مبلغ برابر؛ مبلغ ریال = تومان × ۱۰.
- `/buy/return`: بدون auth و بدون CSRF (`ValidateCsrfToken::except` در AppServiceProvider)
  چون مرورگر با POST از shaparak برمی‌گردد و کوکی Lax همراهش نیست.
- **مسیر قدیمی `/api/buy/callback` با `$verified = true` حذف شد.**
- `zaban:reconcile-payments` هر ۵ دقیقه: تاییدیه‌ی دوباره (تا ۱۸ دقیقه) یا استعلام (بعد از ۲۵).
- درگاه آزمایشی: `IRANKISH_FAKE=true` **فقط با `APP_ENV=local`** اثر دارد.
- ⚠️ `zaban_orders.status` در `schema.sql` ENUM بود و `verifying`/`expired` را رد می‌کرد
  (برگشت از درگاه خطا می‌داد). migration `000006` آن را VARCHAR(16) می‌کند.
  **درس:** جدول‌های `schema.sql` ممکن است ENUM داشته باشند؛ هر مقدار تازه‌ی وضعیت را
  با نوع ستون چک کن.

**روز راه‌اندازی درگاه واقعی:**
1. از ایران کیش: شماره پایانه (۸ رقم)، شماره پذیرنده (۱۵ رقم)، کلمه عبور (۱۶ نویسه)، کلید عمومی PEM.
2. **آی‌پی سرور واقعی را به ایران کیش اعلام کن** — سرویس نشانه با آی‌پی محدود است.
3. `.env` سرور:
   ```
   IRANKISH_TERMINAL_ID=...
   IRANKISH_ACCEPTOR_ID=...
   IRANKISH_PASS_PHRASE=...
   IRANKISH_PUBLIC_KEY_PATH=storage/irankish-public.pem
   IRANKISH_FAKE=false
   ```
   و فایل کلید در `storage/irankish-public.pem` (خارج از `public`!).
4. `APP_URL` باید https و دامنه‌ی واقعی باشد — `revertUri` از `route('buy.return')` ساخته می‌شود.
5. cron سرور (مورد ۷) — بدون آن reconcile اجرا نمی‌شود.
6. **یک خرید واقعی با کمترین مبلغ** (قیمت موقت در پنل) و بررسی: دسترسی فعال شد،
   `zaban_orders.status = paid`، `rrn` ثبت شد؛ بعد قیمت را برگردان.

**۴٫۵ نسخه‌ی نمایشی (سال دمو) — ✅ انجام و تست شد**
درخواست کاربر: ثبت‌نام‌کننده‌ی تازه بلافاصله پی‌وال می‌دید و هیچ کاری نمی‌توانست بکند.
- پنل مدیریت ← «قیمت و تاریخ کنکور» ← پنل «نسخه‌ی نمایشی»: سال دمو (یا خاموش) و
  رشته‌های پیش‌فرض دمو. کنار هر سال تعداد سؤال هر رشته نوشته شده.
- قاعده (تصمیم کاربر): رشته‌ی پروفایل اگر انتخاب شده و خریده نشده ← فقط همان؛
  وگرنه رشته‌های انتخاب مدیر. بدون محدودیت زمانی. خرید یک رشته دموی بقیه را برنمی‌دارد.
- `Entitlements::scope($uid, $exam)` ← `null` کامل، `سال` دمو، `false` هیچ. همه‌ی
  ۹ جای بررسی دسترسی از `scope` / `canItem` / `scopeQuery` می‌پرسند (محتوا، سؤال‌ها،
  پاسخ تکی و دسته‌ای، آمار سؤال، تلاش تمرینی، دک/منتخب/یادداشت، علامت خواندن،
  شروع آزمون). آمار جمعی فقط برای رشته‌ی خریده‌شده (عمدی).
- `ContentBuilder::core/questions($exam, $year)`: محتوای سال‌های دیگر **از سرور بیرون
  نمی‌رود**؛ حتی کاربرد کلمه در سال‌های دیگر (`occ`) هم فقط سالِ دمو. کلید کش دمو
  نسخه‌ی محتوا را دارد (bump خودکار باطلش می‌کند). ETag جدا (`core-y1405`).
- `/me` ← `profile.demo = {year, exams}`؛ محتوا ← `demo_year`؛ نوار «نسخه‌ی نمایشی» بالای صفحه.
- **ربط به امنیت (مورد ۶):** کاربر رایگان دیگر کل بانک را نمی‌گیرد، فقط یک سال.

**۴٫۶ دکمه‌ی همگام‌سازی سؤال‌ها در پنل مدیریت — ✅ انجام شد (تست روی سرور، با azmoon واقعی)**
اپراتورها سؤال‌ها را روی سرور در پلتفرم آزمون وارد می‌کنند؛ مدیر در پنل ←
«همگام‌سازی سؤال‌ها»: «بررسی» (`zaban:sync-questions --check`) و «همگام‌سازی»
(`sync-questions` + `import finalize`). سال، ساختار دفترچه، متن‌ها، سؤال‌ها و گزینه‌ها
با هم می‌آیند؛ سال تازه خودش در همه‌ی فهرست‌ها (و منوی سال دمو) ظاهر می‌شود.
- `ZabanAdminSyncController`: قفل (دو اجرای هم‌زمان نه)، `ignore_user_abort` (بستن صفحه
  کار را نیمه‌کاره نمی‌گذارد)، گزارش آخرین اجرا در کش.
- دفترچه‌ی «ناهمخوان» با بانک کلمات وارد نمی‌شود (رفتار پیش‌فرض دستور؛ `--force` فقط از CLI).
- سالی که هنوز کلمه‌ای در بانک ندارد، بدون مقایسه وارد می‌شود.
- همان صفحه «سال‌هایی که کلمه دارند ولی سؤالشان در سایت نیست» را نشان می‌دهد — گزارش
  دفترچه‌های ناقص داشبورد این‌ها را نمی‌دید (مثل ۱۴۰۵ در لوکال).
- **کلمات سال تازه** (معنی، سطح، مثال) از اکسل می‌آیند، نه از پلتفرم آزمون؛ هنوز با
  `zaban:import words <فایل>` و بعد `zaban:import finalize` و `zaban:predict`.
- **روی سرور:** اتصال `azmoon` در `config/database.php` به دیتابیس پلتفرم آزمون روی
  همان سرور، با کاربر **فقط‌خواندنی**. بعد از دیپلوی یک بار «همگام‌سازی» بزن.

**۵. تست‌های خودکار — ✅ تمام: ۳۸ از ۳۸ سبز (۱۰۹ assertion، ۱۰ ثانیه)** — بعد از هر تغییر اجرا شود.
- باگ واقعی: `ContentBuilder::assertNoAnswerKey` — در `"«$k»"` PHP نویسه‌ی `»` را جزء اسم
  متغیر می‌خواند («Undefined variable $k»») و به‌جای RuntimeException خطای دیگری می‌داد.
  با `{$k}` درست شد. جست‌وجوی کل پروژه برای همین الگو: فقط همین یک مورد.
  **قاعده:** متغیر چسبیده به نویسه‌ی فارسی در رشته‌ی دوگیومه ← همیشه `{$var}`.
- تست اشتباه: `Fsrs::review` فقط زمان‌بندی می‌دهد؛ `lapses`/`again_count` را کنترلر ثبت
  می‌کند. تست دو تا شد: فاصله (Fsrs) و شمارش «یادم نبود» (از `/api/review`).
- `schema.sql` وجود نداشت؛ به‌جایش `php artisan schema:dump` ← `database/schema/mysql-schema.sql`
  (ساختار واقعی فعلی + ردیف‌های جدول migrations). **بعد از هر migration تازه دوباره dump بگیر.**
- تست‌ها روی MySQL دیتابیس **`zaban_test`** (phpunit.xml)، نه SQLite. `beforeRefreshingDatabase`
  اگر اسم دیتابیس چیز دیگری باشد پیش از پاک کردن متوقف می‌کند.
- اجرا (بارگذاری schema به برنامه‌ی `mysql` نیاز دارد):
  ```
  set PATH=C:\wamp64\bin\mysql\mysql9.1.0\bin;%PATH%
  php artisan test --filter=ZabanSecurity
  ```
- `ZabanSecurityTest`: ۳۷ تست — به‌روزشده (خرید از `/buy`، قیمت از `Pricing`) و تازه: دمو،
  قفل پاسخ دسته‌ای، نمره‌ی گزینه‌ی صفرپایه، خرید/برگشت درگاه (موفق، ناموفق، مبلغ دست‌کاری‌شده،
  برگشت دوباره، نشانه‌ی ناشناس)، تبدیل شمسی و تاریخ شمسیِ قدیمی، پروفایل، حذف یادداشت.
- **باگ پیدا شده با schema:** `zaban_profiles.nickname` «NOT NULL، ۲۴، یکتا» بود ← نام مستعار
  خالی، بلندتر از ۲۴، یا تکراری = خطای ۵۰۰. migration `2026_09_22_000002` (NULL‌پذیر، ۳۰)،
  قاعده‌ی `unique` با پیام فارسی، و صفحه حالا پیام ۴۲۲ سرور را نشان می‌دهد.

**۶. امنیت محتوا (ضد سرقت) — در حال انجام**
تصمیم‌های کاربر (۳۱ شهریور ۱۴۰۵):
- ✅ **سقف ۲۰۰ سؤال در روز** برای دیدن پاسخ/تشریحی + هشدار به مدیر (`config/zaban.php`،
  `App\Services\Security\AnswerQuota`). از هر سه راه اعمال می‌شود: پاسخ تکی (۴۲۹)، دسته‌ای
  (`limited`)، و **کارنامه‌ی آزمون** (بدون آن، ساختن و بستن ۴۹ آزمون کل پاسخ‌نامه را می‌داد).
  سؤالِ امروز دیده‌شده دوباره شمرده نمی‌شود.
- ✅ **یک حساب، یک دستگاه**: `SingleSession` (گروه web) + شنونده‌ی `Login`؛ ستون
  `users.session_token`. دستگاه خارج‌شده کوکی `sso_probe` می‌گیرد تا ورود بی‌صدای SSO برش
  نگرداند (وگرنه دو دستگاه بی‌انتها همدیگر را بیرون می‌انداختند). ۵ جابه‌جایی در روز ← هشدار.
- ✅ پنل ← «هشدارهای امنیتی» (`security_alerts`، هر نوع برای هر کاربر روزی یک بار).
- ✅ **باگ پیدا شده با تست:** پایانِ اولِ آزمون خروجی `grade()` را برمی‌گرداند که کلید پاسخ
  خودش را بی‌سقف می‌ساخت (سقف فقط روی `report()` بود). حالا `examFinish` و «مهلت گذشته» در
  `examSave` بعد از `grade()` خروجی `report()` را می‌دهند.
- ✅ **نشست خارج‌شده و درخواست‌های هم‌زمان:** درخواست‌هایی که بعد از بسته شدن نشست می‌رسند
  ۴۰۱ ساده می‌گیرند و رابط خودکار به /login می‌رفت (= ورود دوباره و بیرون انداختن دستگاه دیگر).
  کوکی نشانگر `zaban_replaced` (۱۰ دقیقه، httpOnly=false، مستثنا از رمزنگاری) ← هر ۴۰۱ همان پیام.
- **درخواست کاربر: «کاری کن که نتوانند سرقت کنند».** توضیح صادقانه داده شد (جلوگیری صددرصدی از
  کپیِ چیزی که به خریدار نشان داده می‌شود ممکن نیست) و بسته‌ی پنج‌لایه پذیرفته شد:
  ۱) بانک یک‌جا نه ۲) سقف روزانه ۳) قفل خودکار ۲۴ ساعته روی الگوی اسکریپت + باز کردن از پنل
  ۴) امضای نامرئی به‌ازای کاربر ۵) سخت کردن کپی ساده. ترتیب: مثال‌ها (۱+۲) ← قفل خودکار ←
  معنی‌ها (۱+۲) ← امضا و کپی.
- ✅ **قدم ۱ — مثال‌ها، ✅ تست شد:** `'ex'` از `ContentBuilder::words()` حذف شد؛
  `ContentBuilder::examples()` + `GET /api/words/detail?ids=` (حداکثر ۴۰؛ `filterEntitledItems`
  ← دمو فقط سالِ دمو؛ `WordQuota` سقف `zaban.word_daily_cap`=۱۵۰ کلمه‌ی متفاوت در روز،
  جدول `word_reveals`، هشدار `word_cap`). migration نسخه‌ی محتوا را bump می‌کند. رابط:
  `examplesOf(w, onReady)` در `renderWord` و پشت کارت مرور. ۴۵ تست.
  ✅ تست دستی شد (مثال‌ها در صفحه‌ی کلمه و پشت کارت؛ `content` بی‌مثال).
- ✅ **قدم ۲ — قفل خودکار، ✅ تست شد:** `App\Services\Security\AbuseGuard` بعد از هر دریافت تازه:
  بیش از `lock_words_10min`=۱۰۰ کلمه یا `lock_answers_10min`=۱۰۰ پاسخ «آزاد» در ۱۰ دقیقه، یا
  سقف روزانه‌ی کلمه در `lock_cap_streak`=۳ روز پیاپی ← `users.locked_until` (+`lock_reason`)
  برای `lock_hours`=۲۴ و هشدار `auto_lock`. ادمین/مدیر/ویراستار قفل نمی‌شوند. `AccountLock`
  (گروه web): فقط `/api/*` ← ۴۲۳ `account_locked` با پیام فارسی؛ رابط پیام را نشان می‌دهد.
  پنل ← هشدارهای امنیتی ← «حساب‌های قفل‌شده» با دکمه‌ی باز کردن. ۴۹ تست.
  ✅ تست شد (قفل با پیام دیده شد؛ ۴۹ تست).
- ✅ **قدم ۳ — معنی‌ها، ✅ تست شد:** `'fa'` از بسته حذف؛ `GET /api/words/meanings?ids=` (≤۶۰،
  `MeaningQuota` جدول `meaning_reveals`، هشدار `meaning_cap`، `AbuseGuard::afterMeaningReveal`)
  و `GET /api/words/search?q=` (فقط شناسه، فقط کلمه‌های در دسترس). رابط: **`w.fa` getter است**
  (`meanGet`: کش یا «…» + صف دسته‌ای ۶۰تایی + `meaningsArrived` بازکشی). عوض شد چون روی همه‌ی
  کلمات w.fa می‌خواندند: جست‌وجو ← `meanHit` (فارسی به سرور، ۳۵۰ms مکث)، آزمونک (گزینه‌ها یک
  بار برای هر کارت در `c._quiz`)، `allCards` (کپی معنی حذف)، نمودار (راهنمای میله بی‌معنی)،
  دک و منتخب **صفحه‌به‌صفحه ۵۰تایی**. **قاعده: هرگز w.fa را در حلقه روی WORDS نخوانید.**
- ✅ **پنل ← قیمت و تنظیمات ← «امنیت محتوا»**: همه‌ی سقف‌ها و آستانه‌های قفل و مدت قفل
  (`App\Services\Security\Settings`، کلیدهای `sec_*` در `zaban_meta`، کمینه/بیشینه‌ی هر عدد)،
  و «فهرست‌ها با معنی» (`listFa`). پیش‌فرض معنی: سقف ۱۵۰۰/روز، قفل ۹۰۰ در ۱۰ دقیقه («مطالعه‌ی
  ترتیبی» هر بار ۲۰۰–۳۰۰ معنی). ۵۳ تست.
  ⚠️ باگ اولین نصب: تابع تازه‌ی `repLabel(w)` هم‌اسم تابع قدیمی `repLabel(k)` (برچسب گزارش از
  کلید) بود؛ در JS دومی بی‌صدا جای اولی را می‌گیرد ← «k.charAt is not a function» و صفحه‌ی
  نیمه‌کاره. به `wordRepLabel` تغییر نام داد.
  **قاعده:** پیش از افزودن هر تابع سطح‌بالا به HTML، اسم تکراری را بگرد (اسکریپت بررسی تکرار
  در `<script>`ها؛ الان فقط `show` تکراری است که هر دو در scope جدا و قدیمی‌اند).
  ✅ قدم ۳ تست دستی شد.
- ✅ **قدم ۴ — امضا و کپی، ✅ تست شد:**
  - `App\Services\Security\Watermark`: شماره‌ی کاربر با نویسه‌های بی‌پهنا (U+2064 نشانگر،
    U+200B/2060/2062/2063 رقم مبنای ۴، ۱۲ رقم + رقم کنترل) بعد از اولین فاصله‌ی **هر** معنی،
    مثال (جمله و ترجمه) و پاسخ تشریحی (تکی، دسته‌ای، کارنامه). رفت‌وبرگشت و رد رقم دست‌کاری‌شده
    آزموده شد. پنل ← «ردیابی متن منتشرشده» (`zadmin.leak`): متن را بچسبان ← حساب منبع.
  - درهم‌سازی تب Network: `/me` کلید نشست (`ok`) می‌دهد؛ رابط سرآیند `X-Zaban-Obf: 1` می‌فرستد؛
    `/words/meanings`، `/words/detail`، `/answers` با XOR+base64 (`{o}`) برمی‌گردند. تطبیق بایت‌به‌بایت
    PHP↔JS با متن فارسی و نویسه‌های نامرئی آزموده شد. بدون سرآیند (تست‌ها) پاسخ ساده است.
  - کپی ساده: `user-select:none` + جلوگیری از copy/contextmenu/dragstart روی معنی، مثال و تشریحی.
  - **صادقانه:** متخصص می‌تواند امضا را پاک کند و درهم‌سازی را باز کند (کلید در مرورگر است)؛
    این دو لایه برای همه‌ی کاربران غیرمتخصص است؛ متخصص به سقف، قفل خودکار و هزینه‌ی حساب می‌خورد.
  - ۵۶ تست.
- **امنیت محتوا: هر پنج لایه ✅.**
- ✅ **بانک لغات — راه دوم (تکه‌تکه):** انجام شد (قدم ۱ و ۳ بالا).
- ۴۳ تست.

جزئیات قبلی این بخش: — آخرین کار، به خواست کاربر. هدف واقع‌بینانه:
چیزی که در مرورگر نشان داده می‌شود کاملاً قابل‌محافظت نیست؛ هدف این است که
**برداشتن انبوه سخت، کند و قابل‌ردیابی** شود:
- `/api/content` الان **کل بانک ۷۵۹۴ کلمه** با معنی و مثال را یک‌جا می‌فرستد.
- `/api/answers` (اضافه‌شده در همین دور) با throttle فعلی کل پاسخ‌ها و
  تشریحی‌های ۱۲۳۸ سؤال را در حدود **یک دقیقه** می‌دهد ← سقف روزانه لازم است.
- سقف روزانه‌ی هر کاربر، ثبت و هشدار رفتار غیرعادی (`answer_reveals` از قبل هست).
- واترمارک نامرئی به‌ازای کاربر در متن‌ها تا نشت قابل‌ردیابی باشد.
- یک نشست فعال به‌ازای هر حساب (جلوی اشتراک حساب).
- JS: کوچک‌سازی/مبهم‌سازی (ارزش محدود؛ فقط کند می‌کند).
- هدرهای امنیتی، بستن `/admin/ping` و مسیرهای توسعه.

**۸. آدرس صفحه‌ها و جدا کردن کد (درخواست کاربر: «هر صفحه ویو ندارد، لینک ندارد»)**
به ترتیب پیشنهادی (تأیید کاربر): اول آدرس‌ها، بعد جدا کردن کد به‌تدریج.
- ✅ **آدرس‌ها، ✅ تست شد:** `Route::get('/zaban/{path?}')->where('path','.*')` همان فایل را
  می‌دهد؛ router در HTML (`urlFor` / `syncUrl(push)` / `applyRoute(first)` کنار `showView`):
  `/zaban` داشبورد، `/zaban/<تب>`، `/zaban/word/<کلمه>` (کلمه‌ی دارای «/» ← `?w=`؛ Apache اسلش
  رمزشده را ۴۰۴ می‌کند)، `/zaban/test/<سال>/<ce|it|cs>/<شماره>`، `/zaban/text/<سال>/<رشته>/<cloze|passage>/<p>`.
  آدرس قدیمی `#tab` ← replaceState. باز/بستن جزئیات ← pushState (دکمه‌ی بازگشت مرورگر)؛ بستن با
  کلیک بیرون پنل هم حالا `closeModals()`. همه‌ی آدرس‌های دارایی مطلق‌اند (`/js/…`، `/api`).
  منطق router با مرورگر شبیه‌سازی‌شده از خود فایل آزموده شد. ۵۷ تست.
- ✅ **آدرس بخش‌های داخلی (درخواست دوم: «دیدن کارنامه لینک ندارد؛ همه‌ی بخش‌ها را چک کن»):**
  `/zaban/exam/run` (آزمون در حال اجرا؛ بار اول از `zban_exam` همین مرورگر ادامه می‌دهد)،
  `/zaban/exam/result/<شماره‌ی آزمون>` (منتظر `window.HIST_READY`؛ آدرس در این فاصله دست نمی‌خورد
  تا قدم اضافه در تاریخچه نماند)، `/zaban/review` (همان «مرور امروز»)، `/zaban/<تب>/cards/<کلید>`
  (فهرست کارت‌ها، `CB.key`/`CB_SETS`)، `/zaban/reports/<کلید گزارش>` (گفتگوی پشتیبانی).
  `closeOverlays()` پنجره‌های آدرس‌دار را با رفتن به آدرس دیگر می‌بندد. **عمداً بی‌آدرس:** پنجره‌های
  کاری کوتاه (یادداشت، گزارش تازه، پرسش تأیید، admM) و فیلتر/صفحه‌بندی درون تب‌ها.
  **قاعده:** هر نمای تازه‌ای که ساخته می‌شود، در `urlFor` و `applyRoute` هم ثبت شود و هنگام
  باز/بسته شدن `syncUrl(push)` صدا بزند.
- **جدا کردن کد** (فایل اولیه ۴۴۲KB، ۲۵۱ تابع) — بعد از هر قدم تست خودکار + تست دستی کاربر:
  - ✅ **قدم ۱ — CSS، ✅ تست شد:** تنها بلوک `<style>` (۹۸KB، در head؛ بی `url()`/`@import`؛
    هیچ JS به آن وابسته نبود) عیناً به `public/css/zaban.css`؛ جایش
    `<link rel="stylesheet" href="/css/zaban.css?v=<md5 ده‌رقمی محتوا>">`. **بررسی کلیدی:**
    برگرداندن CSS به جای link فایل اصلی را **بایت‌به‌بایت** بازسازی کرد. HTML: ۴۴۲KB ← ۳۴۴KB.
    **قاعده:** با هر ویرایش `zaban.css` عدد `?v=` هم عوض شود (تست `test_css_version_matches_the_css_file`
    یادآوری می‌کند؛ در قدم Blade خودکار می‌شود). اگر کد با git می‌رود، پایان خط این فایل را
    دست نزنید (autocrlf ← md5 فرق می‌کند).
  - ✅ **هدر موبایل (گزارش کاربر، عرض ۴۱۴):** `.topacts` بی‌شکست + دکمه‌های nowrap بعد از افزوده
    شدن «پلتفرم‌ها/خروج/مدیریت» صفحه را پهن می‌کرد. انتهای `zaban.css`: `@media (max-width:640px)`
    — شکست به خط دوم، دکمه‌های جمع‌تر، «مرور امروز» یک‌خطی، منوی پلتفرم‌ها به عرض ردیف
    (`.psw` static، `.topacts` relative). دسکتاپ دست نخورد. `?v=` به‌روز شد. ⏳ تست دستی
  - ✅ **نوار زمانی کلمه (`.tl`) در موبایل + باگ قدیمی برچسب‌ها:** ۲۵ ستون ثابت ۱۸px (۵۲۲px)
    روی گوشی بیرون می‌زد ← در موبایل ستون‌های `minmax(0,1fr)` و برچسب ۹px. و: `YEARS` از سرور
    می‌آید و همیشه ۲۵ نیست (دفترچه‌ی جاافتاده؛ دمو = ۱ سال) — با ۲۵ ثابت برچسب سال‌ها زیر خانه‌ها
    نمی‌نشستند (دسکتاپ هم). حالا `style="--n:${YEARS.length}"` و `repeat(var(--n,25),…)`.
    بررسی همه‌ی عرض‌های ثابت بیش از ۳۸۰px در CSS: بقیه (`.wrap`، `.sheet`، `.repsheet`، جدول
    رتبه‌بندی داخل جعبه‌ی اسکرول‌دار) امن‌اند. `pr-spark` ~۲۲۵px امن است. ⏳ تست دستی
  - ✅ **لایه‌های پنجره (گزارش کاربر: «یادداشت زیر باکس کلمه می‌رود»):** همه‌ی `.scrim` z-index
    یکسان و noteM/repM/admM/askM در HTML پیش از detail/qview/review ← زیرشان؛ toast و tipbox هم پنهان.
    ⚠️ اصلاح اول کار نکرد: قاعده‌ی قدیمی `#noteM.scrim,#repM.scrim,#askM.scrim,#detail.scrim{10050
    !important}` (قوی‌تر از «فقط شناسه»، و detail هم در آن!) هر دو را هم‌ارتفاع نگه می‌داشت؛ به‌علاوه
    قاعده‌های کهنه‌ی ۴۰۰/۴۶۰/۴۵۰/۵۰. حالا **یک قاعده‌ی نهایی** «شناسه + کلاس + !important» در انتهای
    فایل (آن قاعده‌ی قدیمی حذف شد): صفحه‌ها 10050، پنجره‌ی کاری 10060، askM 10070، toast 10080،
    tipbox 10090 — با یک حل‌کننده‌ی cascade روی فایل نهایی آزموده شد. ✅ تست شد
    **قاعده:** پیش از نوشتن z-index، همه‌ی قاعده‌های موجود آن انتخاب‌گر را بگرد؛ پنجره‌ی تازه = یکی از همین لایه‌ها.
  - ✅ **قدم ۲ — Blade، ⏳ تست دستی:** `public/vocab-platform-v69-wired.html` ←
    `resources/views/zaban.blade.php` (بررسی شد: فایل هیچ `{{ }}` و هیچ `@directive` نداشت، پس
    تبدیل امن بود؛ جز دو خط دارایی، بقیه‌ی فایل بایت‌به‌بایت همان است). مسیر `/zaban/{path?}`
    حالا `view('zaban')`. **نسخه‌ی CSS و JS خودکار** از `filemtime` — `?v=` دستی حذف شد.
    **فایل عمومی حذف شد**: پیش از این پوسته از `/vocab-platform-v69-wired.html` بدون ورود هم
    سرو می‌شد. (هنگام نصب روی سرور/لوکال، فایل قدیمی را دستی پاک کنید؛ zip فایل را حذف نمی‌کند.)
  - ✅ **قدم ۳-الف — کد صفحه به فایل جدا، ⏳ تست دستی:** بلوک بزرگ `<script>` (۳۱۶KB) عیناً به
    `public/js/zaban/app.js` (بایت‌به‌بایت)، با نسخه‌ی خودکار `filemtime`. قالب Blade: ۳۴۵KB ← ۲۹KB،
    فقط HTML. بلوک کوچک اول (نوار خطا و تم) عمداً inline ماند چون باید پیش از هر چیز اجرا شود.
  - ⏳ **قدم ۳-ب — ماژول‌بندی واقعی (تصمیم با کاربر):** کل کد داخل `ZABAN.boot(function(){…})`
    است، پس فضای متغیرها **داخلی** است و چند فایل جدا همدیگر را نمی‌بینند. اندازه‌ی کار:
    ۲۵۴ تابع، ۱۲۴ متغیر مشترک، ۸۶۰ فراخوانی متقابل (پرکاربردها: `fa` ۵۰۵، `EX` ۲۴۰، `F` ۷۹).
    گزینه‌ها: (۱) نگه داشتن وضع فعلی؛ (۲) نام‌فضای صریح `Z.*` و انتقال تدریجی بخش‌به‌بخش؛
    (۳) ES modules + build (Vite). هزینه‌ی ۲ و ۳ زیاد و فایده‌اش فقط برای برنامه‌نویس است.

**۹. لندینگ پیج — ✅ اضافه شد، ⏳ تست دستی**
طراحی کاربر (`landing-zaban.html`) در پروژه: `resources/views/landing.blade.php` +
`public/css/landing.css` + `public/js/landing.js` (CSS و JS بیرون آمدند؛ ۲۰ تا `@media` داخل
Blade دستور شمرده می‌شد و صفحه می‌شکست). اتصال‌ها:
- مسیر `/`: مهمان ← لندینگ، کاربر واردشده ← `/zaban`. `home.blade.php` قدیمی حذف شد.
- **قیمت از `Pricing`** (جدول قیمت پنل، هر سه رشته، با رقم فارسی) به‌جای `[قیمت]`.
- **تاریخ کنکور از `zaban_meta`** (`window.LANDING_EXAM`) به‌جای تاریخ ثابت در JS؛ شمارش
  معکوس صفحه با پنل یکی می‌شود.
- دکمه‌ی «خرید و شروع مرور» ← `route('buy')`؛ لینک «ورود» در نوار بالا؛ نمایش `sso_error`.

**۷. آماده‌سازی دیپلوی — ✅ ابزار و راهنما آماده، ⏳ اجرا روی سرور** (preflight روی لوکال اجرا شد؛ ✗ها همه تفاوت عمدی محیط توسعه)
- **`docs/DEPLOY.md`**: راهنمای قدم‌به‌قدم به ترتیب (۰ تا ۱۴): نیازمندی‌ها، کد، `.env` کامل،
  migrate (بارگذاری schema dump — `mysql` CLI لازم)، انتقال **فقط جدول‌های محتوا** از لوکال،
  کاربر فقط‌خواندنی azmoon، کلید ایران کیش، دسترسی پوشه‌ها و کش، cron، SSO، تنظیمات پنل،
  preflight، یک خرید واقعی، پایش بعد از باز شدن.
- **`php artisan zaban:preflight`**: ✓/⚠/✗ برای APP_ENV/DEBUG/URL، منطقه‌ی زمانی، بسته بودن
  `/dev-login`، کوکی امن، کش تنظیمات، storage، SSO، مدیر با موبایل، درگاه (آزمایشی خاموش، مقادیر،
  کلید PEM معتبر و بیرون از public)، **cron واقعاً اجرا می‌شود** (نبض `schedule_heartbeat` هر دقیقه
  در `routes/console.php`)، اتصال azmoon و فقط‌خواندنی بودن (SHOW GRANTS)، محتوا، تاریخ کنکور، آدرس
  پلتفرم‌ها. کد خروج ۱ اگر ✗. هیچ `env()` بیرون از config نیست (با config:cache امن).
- قبلی: اول **`docs/چکلیست_auth_انتقال_زبان.md`** (سمت auth) و
`INSTALL.md` بسته‌ی `zaban-sso` (سمت زبان)، بعد موارد زیر:
- `config/app.php`: `timezone` = `Asia/Tehran` (لوکال انجام شد؛ روی سرور هم).
- cron: `* * * * * cd /var/www/zaban && php artisan schedule:run`
- اتصال `azmoon` با کاربر فقط‌خواندنی، نه root.
- `APP_DEBUG=false`، `APP_ENV=production`.

**هدر (این دور) — ✅ انجام و تست شد:**
- دکمه‌ی خروج (آیکون) در هدر کنار پروفایل، با تأیید.
- سوییچر «پلتفرم‌ها» با طرح پلتفرم مرور (`review-platform-v32-with-switcher.html`).
  آدرس‌ها در `config/platforms.php` از `.env`: `PLATFORM_PLAN_URL`, `PLATFORM_REVIEW_URL`
  (پیش‌فرض review.konkurcomputer.ir؛ PLAN پیش‌فرض plan.konkurcomputer.ir), `PLATFORM_AZMOON_URL`, `PLATFORM_TAKHMIN_URL`؛
  زبان خودش `APP_URL`. بدون آدرس ← کم‌رنگ و بی‌کلیک (بدون برچسب). دکمه کنار «مدیریت» (پیش از داشبورد). فقط http/https در href.
  **روز انتقال:** آدرس واقعی همه‌ی پلتفرم‌ها در `.env` سرور.

**«کارت سررسید در روز» — ✅ درست و تست شد.** `examPlan().load` = Σ 1/بازه، ولی
کارت هنوز مرورنشده (بازه ۰) با `max(1, 0)` «روزی یک کارت» حساب می‌شد ← دک ۱۲۰۰ کارتی
تازه عدد ۱۲۱۲ و «بار واقعی روز» ۱۲۲۷ می‌ساخت. حالا فقط کارت‌های مرورشده؛ تعداد کارت
تازه‌ی دک (`fresh`) جدا در راهنما گفته می‌شود (روزی حداکثر `NEW_PER_DAY`).

**باگ `cap is not defined` (نوار قرمز خطا):** در متن راهنمای «این عددها یعنی چه؟» در داشبورد،
`${fa(cap)}` نوشته شده بود ولی متغیرش `DAY_CAP` است — از دور «صف مرور امروز» جا مانده بود.
فقط با کلیک روی همان دکمه دیده می‌شد، پس نه بررسی نحوی و نه تست‌های خودکار نمی‌گرفتندش؛
نوار قرمز خطای خود پلتفرم گرفتش. کل فایل برای شناسه‌های تعریف‌نشده‌ی مشابه گشته شد: مورد دیگری نبود.

**کارهای کوچک باز (از بازبینی):**
- **تاریخ کنکور — ✅ انجام و تست شد.** دو ایراد:
  (۱) داشبورد تاریخ ثابت داشت و تاریخ پنل به آن نمی‌رسید ← حالا از `/me` (`exam_date`).
  (۲) **⚠️ باگ مالی:** فرم پنل تاریخ را شمسی می‌خواست و خام ذخیره می‌کرد؛ همین مقدار
  `expires_at` خریدهاست و پایگاه داده «1406-02-16» را سال ۱۴۰۶ **میلادی** می‌خواند ←
  با گذاشتن تاریخ کنکور، هر خرید بلافاصله منقضی می‌شد. حالا `App\Support\Jalali`:
  مدیر شمسی می‌نویسد، میلادی ذخیره می‌شود؛ مقدار شمسیِ قدیمی در `Pricing::accessUntil`
  خودکار تبدیل می‌شود؛ تاریخ گذشته پذیرفته نمی‌شود. تبدیل با ۵ تاریخ معلوم (از جمله
  اسفندهای کبیسه) در هر دو جهت آزموده شد.
- سؤال‌های ۱۴۰۵ در لوکال نیست ← با دکمه‌ی همگام‌سازی (۴٫۶) یا به‌روزرسانی کپی azmoon.
- (اختیاری) آپلود اکسل کلمات سال تازه از پنل، به‌جای خط فرمان.

**موازی (کار تیم محتوا):** ۵ دفترچه‌ی ناقص؛ بعد `zaban:sync-questions` و `zaban:import finalize`.

**تصمیم‌های باز:** سقف ۲۰ کارت تازه در روز (فعلاً ثابت در کد).

---

## 🔴 جزئیات فنی کارهای انجام‌شده و باز

الگوی تکراری همه‌ی این‌ها: **باقیمانده‌ی نسخه‌ی دمو** — چیزی که در
مرورگر ساخته یا ذخیره می‌شد و موقع وصل شدن به سرور جا ماند.

### ح. زمان‌بندی مرور — ✅ انجام و تست شد (۱۶ ← ۱۳ بعد از ۳ مرور در کروم؛ هر دو مرورگر یکی)
سه مشکل با هم بود:
1. زمان‌بندی از `localStorage` خوانده می‌شد و روی سرور می‌نشست. حالا
   `loadServerSched` از `SERVER_DECK` (ستون‌های `deck_items`) می‌سازد؛
   `hydrate` از مرورگر فقط `miss` را می‌خواند (نه دک، نه منتخب، نه زمان‌بندی).
2. **`dayNow` هیچ‌وقت جلو نمی‌رفت** (همیشه ۰) ← هیچ کارتی دوباره سررسید
   نمی‌شد. حالا شماره‌ی روز تقویمی واقعی است (`todayIdx`)، تاریخ سرور با
   `dayOfDate` به همان مقیاس می‌آید، و بعد از نیمه‌شب خودش جلو می‌رود.
3. **سرور: `diffInDays` در Carbon 3 منفی است** ← `elapsed` همیشه ۰ بود و
   FSRS هر مرور را «همان روز» حساب می‌کرد. با حساب ثانیه درست شد. فقط روی
   مرورهای **از این به بعد** اثر دارد.
4. **«مرور امروز» به موعد کارت‌ها نگاه نمی‌کرد**: ۲۰ کارت تصادفی از کل دک،
   و عدد روی دکمه اندازه‌ی کل دک (۹۶۸) بود. حالا `todayQueue()`: اول
   سررسیدها (دیرکردترین اول)، بعد کارت تازه تا `NEW_PER_DAY = 20`، کل صف تا
   `DAY_CAP = 60`. دکمه‌ی بالا، کارت داشبورد و جلسه‌ی مرور همه از همین.
   تعداد کارت تازه‌ی امروز از سرور (`new_today` در `/me`، از `review_logs`).
   - **تصمیم محصولی باز:** عدد ۲۰ فعلاً ثابت در کد است؛ شاید بعداً در پنل
     مدیریت یا تنظیمات کاربر.
- `/me` حالا `last` (تاریخ آخرین مرور) و `new_today` هم می‌فرستد.
- ✅ تست ج: سرور مرورها را درست ثبت می‌کند (`due_date` آینده، `last_review_at` امروز).
- **timezone = UTC بود** (مرور ساعت ۱۹:۳۳ تهران با ۱۶:۰۳ ثبت شد). باید
  `Asia/Tehran` شود، وگرنه «امروز» سرور و مرورگر بین ۰۰:۰۰ تا ۰۳:۳۰ فرق دارد.

### ب. یادداشت‌ها — ✅ انجام و تست شد
سه ایراد با هم: (۱) دکمه‌ی «ذخیره‌ی یادداشت» هیچ‌وقت به سرور نمی‌فرستاد
(`ZABAN.note` جایی صدا زده نمی‌شد)؛ (۲) یادداشت سؤال از سرور با کلید `q:<id>`
می‌آمد و صفحه `q:<سال|رشته|شماره>` می‌خواست؛ (۳) `note()` در بوت‌استرپ از
کلید متنی id عددی درمی‌آورد (NaN). و یک تله‌ی سرور: `body` رشته‌ی خالی را
لاراول null می‌کند، پس حذف یادداشت ۴۲۲ می‌گرفت — `nullable` شد.
- یادداشت‌هایی که قبل از این نسخه فقط در مرورگر بودند منتقل نمی‌شوند (دیتای تست).

### ج + د. پروفایل و رتبه‌بندی — ✅ انجام و تست شد (پروفایل در دو مرورگر؛ یادداشت‌ها ✅؛ کارنامه از تاریخچه ✅)
تصمیم کاربر: دانشگاه/معدل/سهمیه/نوع کنکور **روی سرور ذخیره شوند**؛
نام و موبایل **فقط نمایش** از حساب کاربر.
- migration `2026_09_21_000003_extend_zaban_profiles`: ستون‌های `university`,
  `gpa`, `quota` (free/veteran), `degree` (msc/phd) و timestamps — فقط اگر نباشند.
- `PUT /api/profile` (`ZabanController@profile`)؛ `/me` نام، موبایل (فقط اگر
  ستون `users.mobile` باشد — بعد از SSO) و فیلدهای تازه را می‌فرستد.
- صفحه: هیچ مقدار ثابتی نمانده؛ `PROF` از `profFromServer` (کد رشته ← نام فارسی).
- **رتبه‌بندی خالی بود**: `BuildBoard` و `board()` با JOIN داخلی فقط کاربران
  دارای ردیف پروفایل را می‌آوردند و ردیفی هیچ‌وقت ساخته نمی‌شد. حالا LEFT JOIN
  با پیش‌فرض «نمایش» (همان پیش‌فرض `/me`).
- **ردیف تکراری**: سرور `me: true` می‌فرستد و صفحه ردیف خودش را از سرور حذف
  می‌کند؛ ردیف «شما» با آمار زنده‌ی محلی می‌ماند.

### ی. فعالیت روزانه از سرور — ✅ انجام و تست شد
ردیف «شما» در رتبه‌بندی و روند هفتگی داشبورد از `zban_act` مرورگر می‌آمد؛ در
مرورگر دیگر همه صفر. حالا `/me` فعالیت ۳۷۰ روز اخیر (`activity`، از
`daily_activity`) را می‌فرستد و بوت‌استرپ در `mem.zban_act` می‌گذارد.
`miss` («چند بار یادم نبود») هم از `again_count` سرور ساخته می‌شود؛
`hydrate` دیگر هیچ چیز از `zban_state` نمی‌خواند.

### فهرست کامل آنچه هنوز فقط در مرورگر است (بازبینی کامل `LS.get`)
- `zban_qatt` — آمار شخصی هر سؤال ← **مورد «ز»**
- `zban_remind` — تنظیم یادآور روزانه؛ **عمدی** (اعلان مرورگر)
- `zban_theme` — تم ظاهر؛ **عمدی** (ترجیح همان دستگاه)
- `zban_hist` — فقط پشتیبان؛ با `examHistory()` از سرور جایگزین می‌شود
- بقیه (`zban_notes`, `zban_prof`, `zban_reports`, `zban_exam`, `zban_act`)
  را بوت‌استرپ از سرور در `mem` می‌گذارد.

### هـ + ز. آمار سؤال
- **هـ:** «عملکرد همه‌ی کاربران» روی هر سؤال ساختگی است (`qGlobal`، خود صفحه
  می‌گوید «نمایشی»). مسیر `/api/question/{id}/stats` لازم است، از `exam_answers`.
- **ز:** آمار شخصی («این تست را n بار زده‌اید») فقط در مرورگر (`zban_qatt`).

### ط. فیلتر تب «تست‌های زبان»
`<div id="testsHost">` خالی است؛ فیلتر سال/رشته فقط در تب «کلمات» است و
بی‌صدا روی تست‌ها هم اثر می‌گذارد. یا فیلتر به خود تب بیاید، یا دست‌کم
نشان داده شود که فیلتری فعال است.

### سپس، از لیست قدیم:
- **صفحه‌ی خرید + ایران‌کیش** — ✅ رجوع به مورد ۴.
- **یازده/پنج دفترچه‌ی ناقص** — کار تیم محتوا؛ پنل مدیریت الان ۵ تا نشان می‌دهد.
  بعد: `zaban:sync-questions` و `zaban:import finalize`.
- **تست‌های امنیتی** — نیاز به migration از روی `schema.sql`.
- **cron** روی سرور واقعی: `* * * * * cd /var/www/zaban && php artisan schedule:run`
- **SSO** — ✅ تحویل شد؛ رجوع به مورد ۳ و `docs/چکلیست_auth_انتقال_زبان.md`.
- **یادآور روزانه** فقط در مرورگر کار می‌کند — عمدی و مستند، فعلاً لازم نیست.

---

## نکات فنی مهم

### معماری فایل HTML
۴۰۰ کیلوبایت، دو بلوک `<script>`. کل برنامه داخل `ZABAN.boot(function(){...})`.
سؤال‌ها **با تأخیر** و **به‌ازای هر رشته جدا** بارگذاری می‌شوند
(`ZABAN.questions(code)`)؛ پس موقع اجرای اولیه، `QBYKEY` خالی است.
هر چیزی که در لحظه‌ی باز شدن صفحه به سؤال نیاز دارد باید کلید را از سرور
بگیرد یا منتظر `whenQuestions(e)` بماند.

### کلیدها
- کلمه: متن کلمه (`deck`, `star`) — `WID[w]` ← id
- سؤال در صفحه: `"۱۴۰۴|مهندسی کامپیوتر|۶"` با رقم لاتین (`qKey`, `isQKey`)
- کارت مرور: `"w|<کلمه>"` یا `"q|<کلید سؤال>"`
- گزارش/یادداشت: `"w:<کلمه>"` یا `"q:<کلید سؤال>"`
- **اندیس گزینه در صفحه از صفر است**؛ `correct_option` در دیتابیس از یک.

### پاسخ سؤال‌ها
هیچ‌جا به `Q.ans` بدون `fetchAns` تکیه نکن. `ANS[qid]`:
`undefined` = نپرسیده، `null` = سرور نداد، `{ans, exp}` = دارد؛
`ANS_LOCK[qid]` = آزمون فیدبک باز.

### CSRF / z-index / فایل‌های بزرگ
مثل قبل: `X-XSRF-TOKEN` از کوکی؛ `.scrim` پایه 9999 و پنجره‌های روی‌هم 10050؛
و **هر بار نسخه‌ی فعلی فایل را بفرست** تا اصلاحات قبلی پاک نشود.

### اگر خطای قدیمی در Console دیدی
فایرفاکس خطاهای صفحه‌ی قبل را بعد از رفرش نگه می‌دارد. اول سطل زباله‌ی
Console را بزن، بعد رفرش کن.
